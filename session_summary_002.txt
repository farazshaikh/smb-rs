# SESSION SUMMARY 002 — continuation state

## Objective
Implement full SMB2+SMB3 in smb-rs (Rust). SMB1 is done and working.
Fix remaining SMB1 bugs, then add SMB2 commands phase by phase.

## Build / Test
- cd ~/wrk/smb-rs && cargo build --release --workspace
- Run: pkill -9 -x rustsmb; ./target/release/rustsmb -p 4450 -s public=$PWD/share [-u alice:secret123]
- smbclient NT1: smbclient //127.0.0.1/public -p 4450 -U guest% -N --option='client min protocol=NT1' --option='client max protocol=NT1' -c 'ls'
- Impacket: python3 -c "from impacket.smb import SMB; s=SMB('*SMBSERVER','127.0.0.1',sess_port=4450)"
- Spec text: /tmp/smb.txt (SMB1), pdftotext docs/protocol/smb2_3/MS-SMB2.pdf /tmp/smb2.txt

## CURRENT STATE - WORKING
SMB1 negotiate, auth, tcon, create, read, write, close, delete all green.
SMB2 negotiate + auth + tcon + create + read via impacket.
Crypto unit tests green. Auth suite rc=0.

## KNOWN BUGS
1. FIND_FIRST2 level 0x104 entries return empty names to smbclient.
   Pattern offset was fixed from 10 to 12. Impacket list works now.
   smbclient still fails - need to check response format vs spec section 2.2.8.1.1.
2. WRITE_ANDX payload extraction fixed with named offsets (write_offs module).
   File created on disk with correct size but get returns size 0.
   query_file now calls vfs.stat(h.path) - needs testing.
3. TRANS2 QUERY_FS level 0x3EF rejected as unsupported - smbclient falls back OK.

## NEXT STEPS
1. Verify get returns correct file content after query_file fix
2. Fix FIND_FIRST2 0x104 name encoding for smbclient
3. Add remaining SMB2 commands (QUERY_DIR, QUERY_INFO, SET_INFO etc)
4. SMB2 signing and encryption
5. Update README

## KEY GOTCHAS
- Reader::zstring(base) adds cursor pos internally - never pre-add rd.pos
- CAP_LARGE_READX=0x4000 not 0x40000000; EXT_SEC=0x80000000
- SMB2 header is exactly 64 bytes including 4 reserved after signature
- WRITE_ANDX DataLength@bytes18-19, DataOffset@20-21 in Words area
- TRANS2 subcmd either in Setup[0] or params[0]+pad depending on client
- cargo build --release at workspace root only builds meta pkg unless root has deps
